'use strict'

/* 
+ -> addition
- -> subtraction
* -> multiplication

unary
binary
operand

Exponentiation **
*/

let x = 1
x = -x
console.log(x) // -1, unary negation was applied, An operator is unary if it has a single operand

let z = 1, y = 3
console.log(y - z) // 2, binary minus subtracts values, An operator is binary if it has two operands

console.log(2 ** 2) // 2² = 4